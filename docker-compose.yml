version: "3.9"

services:
  app:
    image: maven:3.9.5-eclipse-temurin-17
    working_dir: /workspace
    volumes:
      - .:/workspace
      - ~/.m2:/root/.m2
      - ./data:/data
    ports:
      - "8080:8080"
    command: mvn -q -Dspring-boot.run.fork=false spring-boot:run